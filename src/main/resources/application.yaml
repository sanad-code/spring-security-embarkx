spring:
  application:
    name: spring-security-embarkx
  security:
    user:
      password: 123
      name: msanad
  datasource:
    url: jdbc:mysql://mysql:3307/notes
    username: root
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect

#  h2:
#    console:
#      enabled: true
#
#  datasource:
#    url: jdbc:h2:mem:test